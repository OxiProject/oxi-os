# Oxi OS installer

Anaconda-style graphical installer for Oxi OS (Debian Sid, GNOME,
XFS-only root).

## Layout

- `bin/oxi-installer` — launcher
- `oxi_install/` — `partitioning`, `usersetup`, `install`, `gui`
- `install-oxios.desktop` — live-session desktop entry
